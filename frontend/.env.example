# Backend sunucusunun adresi (bilgisayarınızın LAN üzerindeki IP'si + port).
# Backend server address (your computer's LAN IP + port).
#
# Kullanmak için bu dosyayı ".env" olarak kopyalayın ve kendi IP'nizle güncelleyin:
# To use, copy this file to ".env" and update it with your own IP:
#
#   cp .env.example .env
#
EXPO_PUBLIC_BACKEND_URL=http://192.168.1.106:8000
